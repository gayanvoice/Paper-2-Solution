namespace azure.gremlin.cli.Enums
{
    public enum ResultTypeEnum
    {
        STRING_RESULT,
        INTEGER_RESULT,
        LONG_RESULT,
        VERTEX_OBJECT,
        EDGE_OBJECT,
        ARRAY_RESULT,
        UNKNOWN_RESULT,
        NO_RESULT
    }
}